---
{% for colour in colours %}

  Colour number {{ loop.index }} is {{ colour.name }}.
    people call {{ colour.name }} their favourite.
    And the following are examples of things that are {{ colour.name }}:
    {% for item in colour.things %}
    - {{ item }}
    {% endfor %}

{% endfor %}
