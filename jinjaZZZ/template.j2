{% if modelo_equipo not in modelos_soportados_y_version  | map(attribute='equipo') %}
{{ ' -- EL EQUIPO NO ESTA DENTRO DE LOS EQUIPOS SOPORTADOS -- ' | center }}
{% endif %}


{% for item in modelos_soportados_y_version %}
{{item.equipo}} - {{item.version}}
{% endfor %}

{{ modelos_soportados_y_version  | map(attribute='equipo') | join('\n') }}

{{ modelos_soportados_y_version  | map(attribute='equipo') | tojson }}


{{ modelos_soportados_y_version  | map(attribute='version') | unique | join(', ') }}
